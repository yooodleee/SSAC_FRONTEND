import type { RequestHandler } from 'msw';

// 도메인별 핸들러를 여기에 추가합니다.
// 예) import { authHandlers } from './auth.handler';
//     export const handlers: RequestHandler[] = [...authHandlers];

export const handlers: RequestHandler[] = [];
